# Langfuse stack — local PoC defaults. Replace before any non-local exposure.
LANGFUSE_DB_PASSWORD=langfuse
LANGFUSE_NEXTAUTH_SECRET=replace-with-32-byte-random
LANGFUSE_SALT=replace-with-32-byte-random
LANGFUSE_PUBLIC_URL=http://localhost:3000
